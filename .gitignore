# Claude files — never push
.claude/
CLAUDE.md

# Internal strategy — never push
WINNING_PLAN.md

# Environment — secrets
.env

# Hardhat artifacts — committed so Railway skips resolc compilation
node_modules/
# artifacts/ — intentionally tracked
cache/
typechain-types/

# Coverage
coverage/
coverage.json

# Build
dist/

# Oracle venv
oracle/.venv/

# Lock files — using npm only, ignore pnpm/yarn lockfiles
pnpm-lock.yaml
yarn.lock

# OS
.DS_Store
Thumbs.db
