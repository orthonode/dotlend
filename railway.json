{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --legacy-peer-deps"
  },
  "deploy": {
    "startCommand": "npx hardhat run scripts/generate-solvency-proof.js --network polkadotHubTestnet",
    "restartPolicyType": "NEVER"
  }
}
