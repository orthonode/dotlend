{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --legacy-peer-deps"
  },
  "deploy": {
    "startCommand": "npx hardhat --config hardhat.config.cjs run scripts/generate-solvency-proof.js --network polkadotHubTestnet",
    "restartPolicyType": "NEVER"
  }
}
